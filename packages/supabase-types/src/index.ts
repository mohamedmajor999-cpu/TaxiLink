// Types DB auto-generes via `supabase gen types typescript --linked`.
// La generation reste pilotee depuis le repo (voir scripts/), le fichier
// `database.types.ts` est ecrit ici puis consomme par tous les apps.

export * from './database.types'
