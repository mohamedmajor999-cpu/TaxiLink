// @taxilink/ui-mobile — composants RN partages entre apps/mobile-driver,
// apps/mobile-patron et apps/mobile-patient.
//
// Empty pour le moment. Les composants sont extraits ici quand ils sont
// utilises dans 3 endroits ou plus, suivant la regle des 3 (cf. CLAUDE.md).
//
// Composants prevus (cf. MOBILE_STRATEGY.md §10.4) :
// - Button       : variants primary/secondary/ghost/danger
// - Card         : conteneur arrondi avec shadow
// - Sheet        : bottom sheet modal
// - TextInput    : input avec label flottant + error state
// - Icon         : wrapper @expo/vector-icons
// - Toast        : notifications ephemeres + ToastHost
// - Badge        : pastille coloree
// - Avatar       : photo profil avec fallback initiales
// - Chip         : filtre tap-able
// - EmptyState   : illustration + texte + CTA
// - Loader       : skeleton + spinner
// - MapView      : wrapper react-native-maps avec style TaxiLink
// - BottomBar    : barre nav 5 onglets
// - Header       : en-tete ecran avec back + actions
// - MissionRow   : item de liste mission compact

export {}
