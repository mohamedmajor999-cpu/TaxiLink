# TODO — 2 mai 2026

Tests à faire en prod après le déploiement des phases P0 → P5 + P2 + polish.

---

# 🛰️ CHANTIER — Suivi GPS automatique des courses

> **Statut Phase 1** : livrée 2026-05-02 (commits 69aed01, a6d7a97). Migration appliquée. Hook `useAutoMissionProgress` actif sur le DriverDashboard. Section admin "Suivi GPS automatique" en place.
>
> **Vrai test conditions réelles** : pas encore fait. Tant qu'un chauffeur n'a pas fait une vraie course depuis un mobile dehors, on ne sait pas si ça fonctionne. Le dashboard admin (% suivi complet) le dira sous 1-2 semaines.

## ✅ Quick wins en cours d'implémentation

- [x] **A — Badge "GPS actif" dans le hero chauffeur** (vert/jaune/rouge selon état permission + signal)
- [x] **B — Boutons fallback "Patient déposé" + "Course terminée" dans le hero** (filet de secours si GPS rate)

## 📋 À faire ensuite — par ordre de priorité

### Phase 1.1 — robustification immédiate
- [ ] **C — Onboarding GPS one-shot** : modale au 1er login chauffeur expliquant pourquoi activer la géoloc, avec un bouton "Activer" unique (pas le popup nu du navigateur)
- [ ] **D — Validation lat/lng obligatoire à la création de mission** (côté serveur `/api/missions` POST) — refuse les missions sans coords pour garantir 100% éligibles au suivi auto

### Phase 1.2 — robustesse données
- [ ] **E — Supabase realtime sur la mission courante** dans `useAutoMissionProgress` (subscribe aux changements DB) → synchro instantanée si admin/cron modifient en parallèle
- [ ] **F — Multi-courses du jour** : aujourd'hui le hook ne suit que la `IN_PROGRESS` courante. Pour un chauffeur qui a 5 courses queued, basculer auto sur la suivante après complete.
- [ ] **G — Dwell adaptatif** : 1 min en zone urbaine, 3 min en hôpital indoor (où GPS est mauvais). Heuristique simple : si accuracy > 25m pendant >30s d'affilée, on étend le dwell.
- [ ] **H — Précision GPS visible** dans le badge (chiffre en m) + couleur dégradée selon précision

### Phase 1.3 — visibilité admin
- [ ] **I — Anomalies auto-flaggées** côté dashboard admin : courses où `arrived_at_pickup_at` posé mais `pickup_at` jamais (chauffeur bloqué ?), ou écart > 30 min entre ces deux jalons
- [ ] **J — Filtre "à investiguer" dans la table GPS** : ne montrer que les courses avec ≥ 1 jalon manquant
- [ ] **K — Drilldown par chauffeur** : cliquer un chauffeur dans la table → voir ses 30 dernières courses et son taux de suivi perso

### Phase 2 — fonctionnalités au-delà du suivi
- [ ] **L — SMS auto patient** "votre chauffeur arrive dans 2 min" quand chauffeur entre rayon 500m du pickup
- [ ] **M — Notif chauffeur "arrivée bientôt"** + checklist (signature, voucher CPAM) à 500m de la destination
- [ ] **N — Background tracking via Service Worker + Push API** (continue à suivre quand l'onglet est fermé) — gros chantier (4-5j) car PWA + tests devices nombreux
- [ ] **O — Table `mission_events` d'audit** : journaliser chaque transition (timestamp, lat, lng, accuracy) — utile pour litiges CPAM
- [ ] **P — Métriques continues** : vitesse moyenne, retard live vs `scheduled_at`, distance réelle vs estimée (admin dashboard)
- [ ] **Q — Détection auto patient absent** : si chauffeur stationne >5 min au pickup mais position du patient (si on l'avait) ne bouge pas → propose markNoShow auto. Délicat heuristiquement.

### Tests à faire en conditions réelles (impératif avant Phase 2)
- [ ] Mobile + GPS extérieur : course de bout en bout, vérifier les 5 timestamps remplis dans Supabase
- [ ] Mobile + GPS faible (intérieur hôpital) : voir comment le dwell adaptatif (G) doit se comporter
- [ ] Permission GPS refusée : vérifier que le badge (A) passe rouge et que les boutons fallback (B) fonctionnent

---

## 🎯 Étape 5 — Wizard de course en cours

**Préparation** : tu as besoin de 2 comptes chauffeurs (toi + un autre device, ou toi connecté sur 2 navigateurs).

### Setup
- [ ] **Compte 1** : poster une course test depuis l'accueil
  - Type : peu importe (CPAM ou Privé)
  - Départ + destination : n'importe quoi
- [ ] **Compte 2** : accepter la course depuis le feed

### Sur l'écran "Course en cours" — vérifier la présence de
- [ ] 📊 **Stepper 4 étapes visible** : Acceptée → En route → À bord → Déposé
- [ ] 📍 **Section GPS "Suivi automatique GPS"** avec bouton "Activer"
- [ ] (si course CPAM) Section **"Preuves CPAM"** avec 2 tuiles (signature + bon de transport)

### Cliquer le bouton primaire et vérifier la progression
- [ ] Clic "Je pars chercher le patient" → étape passe à **En route** ✓
- [ ] Clic "Patient à bord" → passe à **À bord** ✓
- [ ] Clic "Patient déposé" → passe à **Déposé** ✓
- [ ] Clic "Course terminée" → retour à l'accueil chauffeur ✓

### Vérification post-course
- [ ] Onglet "Mes courses" → sous-onglet "Historique" → la course test apparaît bien dans la liste

---

## 🎯 Étape 6 — Tests des features avancées

### Bouton "Patient absent" (no-show)
- [ ] Sur une course en cours étape **En route**, voir le petit lien gris **"Le patient n'est pas là"**
- [ ] Cliquer → modal motif s'ouvre avec 5 options
- [ ] Choisir "Patient absent au point de RDV"
- [ ] Cliquer "Confirmer l'absence"
- [ ] La course se ferme, retour à l'accueil
- [ ] Vérifier dans Supabase Dashboard → Table `missions` que la course a bien `no_show = true`

### Signature CPAM (uniquement courses type CPAM)
- [ ] Sur une course en cours CPAM, cliquer la tuile **"Signature patient"**
- [ ] Modal canvas s'ouvre
- [ ] Dessiner une signature au doigt (ou souris si desktop)
- [ ] Cliquer "Valider"
- [ ] La tuile passe en vert ✓
- [ ] Vérifier dans Supabase Dashboard → Storage → bucket `mission-evidence` qu'un fichier `<missionId>/signature.png` est créé

### Photo bon de transport CPAM
- [ ] Sur la même course CPAM, cliquer la tuile **"Bon de transport"**
- [ ] La caméra arrière du téléphone s'ouvre (ou la galerie)
- [ ] Prendre une photo (ou choisir un PDF)
- [ ] La tuile passe en vert ✓
- [ ] Vérifier dans Supabase Storage qu'un fichier `<missionId>/voucher.<ext>` est créé

### GPS auto-completion (test terrain — optionnel mais important)
- [ ] Sur une course en cours, activer **"Suivi automatique GPS"**
- [ ] Autoriser la permission GPS quand demandée
- [ ] Le statut passe à "Recherche du signal…" puis affiche la précision (±X m)
- [ ] Faire un vrai trajet (à pied ou en voiture) jusqu'au point de pickup
- [ ] Rester **2 minutes immobile** dans un rayon de 80m du pickup
- [ ] L'app doit basculer automatiquement à l'étape **À bord** (sans clic) ✓
- [ ] Continuer le trajet jusqu'à destination
- [ ] Rester **2 minutes immobile** dans un rayon de 100m de la destination
- [ ] L'app doit basculer automatiquement à l'étape **Déposé** ✓

---

## 🚨 Si ça plante

Note l'erreur ici :

```
[copie-colle l'erreur ou la description du bug]
```

**Causes les plus probables :**
- Bouton qui ne fait rien → bug client-side, ouvrir la console (F12) sur desktop
- "Permission denied" sur signature/photo → migration P5 pas appliquée OU bucket `mission-evidence` inexistant
- Étape qui n'avance pas → migration P1 pas appliquée (colonnes manquantes)

---

## ✅ Une fois tout vert

Tout valide → on peut s'attaquer à la migration **MapLibre + OpenFreeMap** (option C, ~1 jour) pour fixer les labels qui tournent.

---

# 🏢 CHANTIER — Dashboard Patron de flotte (multi-tenancy)

> **But** : transformer la maquette `/dashboard/patron-mockup` (UI seulement, données fake) en un vrai dashboard branché à Supabase, multi-tenant, scalable.
>
> **Décisions validées (2026-05-02)** :
> - Mohamed = `owner` d'une org "TaxiLink Default" qui contient tous les drivers/missions existants (peut être à la fois owner + driver)
> - Multi-org per user : oui, table `organization_members` (un user N orgs)
> - Backfill : tous les drivers/missions existants rattachés à "TaxiLink Default"
> - Stratégie de migration : **Supabase CLI local + Docker** (bonne pratique pro, zéro risque sur prod)
> - Auth : on garde Supabase Auth + RLS pour le MVP (migrer vers WorkOS quand 1er patron demande SSO)
> - Stack : Drizzle/tRPC reportés — on reste cohérent avec l'existant (Supabase JS + services)

**Estimation totale** : 3-5 jours étalés. Phase 1 = ~30 min toi (install), reste = code Claude.

---

## 🎯 Étape A1 — Installation environnement local pro (~30 min, une fois)

### Docker Desktop (Windows 11)
- [ ] Télécharger depuis https://www.docker.com/products/docker-desktop/
- [ ] Installer en cochant "Use WSL 2 instead of Hyper-V"
- [ ] Redémarrer l'ordi (obligatoire 1ère fois)
- [ ] Lancer Docker Desktop, skip la connexion (pas besoin de compte)
- [ ] Attendre que la baleine 🐳 dans la barre des tâches soit stable
- [ ] Vérification : `docker --version` retourne `Docker version 27.x.x` ou +

### Supabase CLI via Scoop (méthode officielle Windows)
Dans **PowerShell** (pas bash) :
- [ ] `Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser`
- [ ] `irm get.scoop.sh | iex`
- [ ] `scoop bucket add supabase https://github.com/supabase/scoop-bucket.git`
- [ ] `scoop install supabase`
- [ ] Vérification : `supabase --version` retourne `2.x.x`

### Infos Supabase à récupérer
Dashboard https://supabase.com/dashboard/project/ivumykufinlniffxqlud :
- [ ] Project Ref : `ivumykufinlniffxqlud` (déjà connu)
- [ ] DB password : Settings → Database → "Database password" (reset si oublié, **noter dans un endroit sûr**)
- [ ] Service role key : Settings → API → `service_role` secret (utile pour scripts admin plus tard)

---

## 🎯 Étape A2 — Init Supabase local + clone schema prod

### Initialisation
- [ ] Depuis racine TaxiLink : `supabase init`
- [ ] Vérifier création du fichier `supabase/config.toml`
- [ ] Si `apps/web/supabase/migrations/` existe (5+ migrations), les déplacer vers `supabase/migrations/` à la racine (convention Supabase CLI)

### Lien projet distant
- [ ] `supabase login` (ouvre navigateur, se connecter avec le compte qui possède le projet)
- [ ] `supabase link --project-ref ivumykufinlniffxqlud` (demande le DB password)

### Clone schema prod
- [ ] `supabase db pull` → génère un fichier `supabase/migrations/<timestamp>_remote_schema.sql` qui reflète l'état actuel de prod
- [ ] Lire ce fichier pour vérifier : tables `profiles`, `drivers`, `missions`, `groups`, `mission_groups`, `driver_documents`, `payments` toutes présentes
- [ ] `supabase start` → lance Postgres local (Docker doit tourner)
- [ ] `supabase db reset` → applique toutes les migrations en local
- [ ] Vérifier : `supabase status` affiche les URLs locales (DB sur `54322`, Studio sur `54323`)

### Vérif sanité
- [ ] Ouvrir http://localhost:54323 (Studio local)
- [ ] Voir les tables apparaître, vides
- [ ] Si on veut des données réalistes : `supabase db dump --data-only -f seed.sql` depuis prod, puis `psql` import en local (optionnel, on peut tester avec base vide + fixtures)

---

## 🎯 Étape A3 — Migration `20260427_organizations.sql`

### Contenu de la migration (à écrire ensemble)
- [ ] Table `organizations` (id, name, siret, created_at, plan, stripe_customer_id NULL)
- [ ] Table `organization_members` (org_id, user_id, role CHECK in owner/admin/dispatcher/accountant/viewer, joined_at)
- [ ] Index sur `organization_members(user_id)` pour la fonction `current_org_ids()`
- [ ] Colonne `organization_id UUID REFERENCES organizations` sur `drivers` + `missions`
- [ ] Index sur `drivers(organization_id)` et `missions(organization_id)`
- [ ] Fonction `current_org_ids()` SECURITY DEFINER qui retourne les org_ids du user courant
- [ ] Policies RLS sur `organizations`, `organization_members`, `drivers`, `missions` : `org_id IN (SELECT current_org_ids())`
- [ ] **Backfill en transaction** :
  - INSERT 1 org "TaxiLink Default" (id fixe ou généré)
  - INSERT Mohamed dans `organization_members` avec role='owner'
  - UPDATE drivers SET organization_id = <default_org>
  - UPDATE missions SET organization_id = <default_org>
- [ ] Tout wrappé `BEGIN; ... COMMIT;` (rollback si une seule étape échoue)

### Tests en local
- [ ] `supabase db reset` → applique la migration sur la base locale
- [ ] Studio local : vérifier tables `organizations`, `organization_members` créées
- [ ] Studio local : vérifier `drivers.organization_id` et `missions.organization_id` peuplés
- [ ] Tester RLS : se connecter avec un user qui n'est pas membre → `SELECT * FROM missions` doit retourner 0 ligne
- [ ] Tester que Mohamed (owner) voit bien tout

### Push vers prod
- [ ] `supabase db push` → applique la migration en prod
- [ ] Vérifier dans Studio prod (https://supabase.com/dashboard/project/ivumykufinlniffxqlud) que tout est créé
- [ ] **Si problème** : `supabase db reset --linked` n'est PAS une option en prod (efface tout). Plan B = restore depuis snapshot Supabase Free (7j de rétention auto)

### Régénération des types TypeScript
- [ ] `supabase gen types typescript --linked > apps/web/src/lib/supabase/types.ts`
- [ ] Vérifier que les types `organizations`, `organization_members` apparaissent
- [ ] `npm run type-check` doit passer

---

## 🎯 Étape A4 — Hook `useCurrentOrg` + middleware

- [ ] Créer `apps/web/src/hooks/useCurrentOrg.ts` : retourne `{ orgId, role, isLoading, isPatron }` pour le user connecté
- [ ] Créer `apps/web/src/services/organizationService.ts` : `getCurrentMemberships(userId)`, `getOrgById(id)`
- [ ] Modifier `apps/web/src/middleware.ts` : matcher `/dashboard/patron/:path*`, vérifier que le user a au moins un membership avec role IN (owner, admin, dispatcher, accountant, viewer)
- [ ] Si user pas membre → redirect `/dashboard/chauffeur` (silencieux, pas d'erreur 403)
- [ ] Tests : `useCurrentOrg.test.ts`, mock du membership

---

## 🎯 Étape A5 — Page `/dashboard/patron`

- [ ] Créer `apps/web/src/app/dashboard/patron/page.tsx` (server component, vérifie auth + membership)
- [ ] Créer `apps/web/src/components/dashboard/patron/PatronDashboard.tsx` (orchestrateur, copie de `PatronDashboardMockup`)
- [ ] Garder `/dashboard/patron-mockup` accessible en parallèle pendant la transition (référence visuelle)
- [ ] Sidebar + bottom nav réutilisés depuis `patron-mockup/PatronSidebar.tsx`

---

## 🎯 Étape A6 — Services patron (vraies données)

### `patronStatsService.ts`
- [ ] `getOverviewKPIs(orgId)` retourne `{ driversOnline, driversTotal, activeMissions, todayRevenue, weekRevenue, monthRevenue, cpamPending, alerts }`
- [ ] Requêtes : COUNT drivers WHERE organization_id=$1 AND is_online=true / COUNT missions WHERE status='in_progress' / SUM price_eur WHERE completed_at >= today / etc.
- [ ] Utiliser une seule fonction Postgres `get_org_overview_kpis(org_id)` pour atomicité (1 round-trip au lieu de 8)

### `patronDriversService.ts`
- [ ] `getFleetPositions(orgId)` retourne `{ id, name, initials, status, vehicle, lat, lng, todayMissions, todayRevenue, rating, alerts }[]`
- [ ] Note : vérifier si `current_lat`/`current_lng` existent déjà sur `drivers` (cf. migration `20260429_drivers_current_position.sql` repérée). Si oui, on les utilise directement.

### `patronActivityService.ts`
- [ ] `getRecentActivity(orgId, limit=20)` retourne événements récents (acceptations, complétions, refus, alertes docs)
- [ ] Source : agrégation depuis `missions` (changements de status timestampés) + `driver_documents` (expiry < 30j)
- [ ] Cap à 20 lignes, ordre desc par timestamp

### `patronDocAlertsService.ts`
- [ ] `getDocAlerts(orgId)` retourne `driver_documents` JOIN `drivers` JOIN `profiles` WHERE expiry_date < now() + 60 days, ordered by daysLeft asc

### Tests
- [ ] `patronStatsService.test.ts` (mock supabase client)
- [ ] `patronDriversService.test.ts`
- [ ] `patronActivityService.test.ts`
- [ ] Couverture cible : 80% (cf. CLAUDE.md)

---

## 🎯 Étape A7 — Hook `usePatronOverview` + realtime

- [ ] `apps/web/src/components/dashboard/patron/overview/usePatronOverview.ts`
- [ ] Charge les 4 services en parallèle au mount
- [ ] Subscribe `missions` realtime scopé `organization_id=eq.<orgId>` → invalide cache KPI sur change
- [ ] Coalesce les events 200ms (sinon re-render 50×/sec)
- [ ] Optimistic updates sur acceptation/complétion

---

## 🎯 Étape A8 — Brancher `PatronOverview.tsx`

- [ ] Remplacer imports `mockStats`, `mockDrivers`, `mockActivity`, `mockDocAlerts` par les retours de `usePatronOverview()`
- [ ] Gérer états `isLoading` (skeleton) et `error` (toast)
- [ ] Vérifier que la vue se charge en < 1.5s (mesurer avec React DevTools Profiler)
- [ ] Tester en mode nuit (compatibilité `night.*` palette)

---

## 🎯 Étape A9 — Validation finale

### Qualité code
- [ ] `npm run type-check` zéro erreur
- [ ] `npm run lint` zéro erreur
- [ ] `npm run test` 848+ tests passent (compter les nouveaux)
- [ ] Aucun fichier ne dépasse les seuils CLAUDE.md (200/150/100 lignes)

### Test fonctionnel manuel
- [ ] Se connecter avec mohamed.major999@gmail.com
- [ ] Aller sur /dashboard/patron → la page s'ouvre (pas de redirect)
- [ ] Voir les KPIs réels (probablement 0 partout au début, c'est normal)
- [ ] Créer manuellement 1 driver online + 1 mission via Studio → voir les compteurs s'incrémenter en realtime
- [ ] Logout, reconnexion avec un compte qui N'EST PAS membre → /dashboard/patron redirige vers /dashboard/chauffeur

### Mise à jour PROGRESS.md
- [ ] Déplacer "Dashboard patron — Phase 1 multi-tenancy" en section ✅ Terminé
- [ ] Garder en ⏳ : Stripe Billing B2B, séparation `apps/patron/`, validation marché 5 patrons

---

## 📌 Hors-scope de ce chantier (pour plus tard)

Ces points seront traités dans des chantiers ultérieurs, **après validation marché** (5 patrons pré-payeurs) :

- Séparation `apps/patron/` du monorepo (workspace Next.js dédié)
- Stripe Billing B2B + onboarding self-serve
- Migration vers WorkOS pour SSO/SAML (quand 1er patron enterprise demande)
- Drizzle ORM + Server Actions (refacto cohérence stack)
- Inngest pour jobs CPAM (génération facture mensuelle)
- Module `analytics/` (BI, top chauffeurs, prédictions CA)
- API publique pour patrons gros (intégrations Sage/QuickBooks)
- Onglets `agenda`, `courses`, `drivers`, `finances` (pour l'instant seul `overview` est branché — les 4 autres restent en mock data dans `patron-mockup/`)

---

## 🚨 Si ça plante pendant le chantier

Note l'erreur ici :

```
[copie-colle l'erreur ou la description du bug]
```

**Causes les plus probables** :
- `docker: command not found` → Docker Desktop pas démarré, ouvrir l'app
- `supabase: error: failed to connect to local Postgres` → `supabase start` jamais lancé OU port 54322 occupé
- `supabase db push` rejeté → migration locale plus à jour que prod, faire `supabase db pull` d'abord
- Type-check casse après `gen types` → les services existants utilisent des `Driver`/`Mission` qui ont gagné `organization_id` (NULL temporairement) → adapter les types `@taxilink/core`
- RLS bloque tout (même Mohamed) → vérifier qu'il est bien dans `organization_members` avec role='owner', re-checker la fonction `current_org_ids()`
